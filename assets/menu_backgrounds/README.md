# Menu Backgrounds

Put main menu background images in this folder.

Supported formats: `.png`, `.jpg`, `.jpeg`, `.webp`.

The menu picks one background on every launch and avoids repeating the previous one when there is more than one image.
